{
  "name": "truecarry-coursebuilder",
  "version": "2.0.0",
  "type": "module",
  "description": "Offline pipeline: OSM + USGS elevation + satellite imagery → course JSON",
  "scripts": {
    "build":         "node build.js",
    "build:refresh": "node build.js --refresh"
  },
  "dependencies": {
    "jimp": "^0.22.12"
  }
}
