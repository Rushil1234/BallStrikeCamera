# Supabase custom SMTP secrets, read by supabase/config.toml via env(...).
# Copy to .env (git-ignored) and fill from your domain-verified SMTP provider
# (Resend, Postmark, SendGrid, AWS SES, ...). See supabase/AUTH_EMAIL_SETUP.md.
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
